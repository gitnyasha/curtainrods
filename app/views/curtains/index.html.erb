		<section class="intro-section">
		<div class="container text-center">
			<div class="row">
				<div class="col-xl-10 offset-xl-1">
					<h2 class="section-title">Quality curtains</h2>
				</div>
			</div>
		</div>
	</section>
    <div class="container-fluid p-md-0 ">
			<div class="row portfolios-area">
          <% @curtains.each do |curtain| %>
				<div class="mix col-lg-6 col-md-6 web">
            <%= image_tag(curtain.image_url(:default), class: "portfolio-item set-bg") %>
				</div>
			    <%= curtain.name %>
      <%= curtain.description %>
      <%= curtain.price %>
      <%= link_to "Show", curtain %>
      <% if user_signed_in? %>
      <%= link_to "Edit", edit_curtain_path(curtain) %>
      <%= link_to "Destroy", curtain, method: :delete, data: { confirm: "Are you sure?" } %>
      <% end %>
          <% end %>
			</div>
		</div>
	</section>
<% if user_signed_in? %>

<%= link_to "New Curtain", new_curtain_path, class: "site-btn" %>

<% end %>
</div>